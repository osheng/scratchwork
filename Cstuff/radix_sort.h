void radix_sort(unsigned int *arr, unsigned int length);
static void paritition(unsigned int *arr, int on, unsigned int from, unsigned int to);
static void copy_arr(unsigned int *buf, unsigned int *arr,
                     unsigned int from, unsigned int to);
